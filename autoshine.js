// const elements = document.querySelectorAll(".btn-primary");
// // const beforeStyles = window.getComputedStyle(elements, "::before");

// elements.forEach((el) => {
//   setInterval(() => {
//     el.classList.add("shine");

//     // Remove the class after the transition ends (e.g., 0.6s)
//     setTimeout(() => {
//       el.classList.remove("shine");
//     }, 6000); // Adjust this to match the transition duration
//   }, 5000); // 5-second interval
// });
